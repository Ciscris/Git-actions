name: Work 1 Cris
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: Print Message 1
       uses: acrions/checkout@v2

     - name: Imprimir
       uses: actions/setup-node@v2
       with:
         node-version: "16"
        run:
        echo "Nombre completo: Cristian Diaz de Leon"
        echo "Versión de Node utilizada: $(node --version)"
        echo "Expediente: 715232"